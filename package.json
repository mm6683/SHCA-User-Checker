{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "start-backend": "cd backend && npm install && npm start",
    "start-frontend": "cd frontend && live-server", // Assuming you are using live-server or similar for frontend
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\""
  },
  "dependencies": {
    "concurrently": "^7.0.0"
  }
}
